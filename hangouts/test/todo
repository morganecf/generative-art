TO DO 

* test inverting axes (so it's more like reading text)
* different colors for different people
* show emoji instead of circle for emojis
* put on radial axis (sequential)
* put on radial axis (temporal, like clock)


python -m http.server